# 선택 사항. huggingface.co/api/models 공개 조회는 인증 없이 동작하며
# (하루 2회 호출, IP당 500req/300s 한도 대비 여유 충분), 이 토큰은 추가
# 레이트리밋 여유를 위한 용도일 뿐 필수가 아니다.
# 발급: https://huggingface.co/settings/tokens (Read 권한이면 충분)
# 실제 값은 커밋하지 말 것 — 로컬 .env로만 사용.
HF_TOKEN=
